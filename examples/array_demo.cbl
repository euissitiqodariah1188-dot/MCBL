// McBL# v2.0 — Array Demo

inc(array_demo);

    // $array syntax (dari spec asli)
    $array murid = {dafa, davin, gerrard, folk}
    
   if murid == dafa;
       pr("dafa")
   if murid == davin;
       pr("davin")
   if murid == folk;
       pr("folk")
   if murid == gerrard;
       pr("gerrard")

endinc;
