inc(multi class code);

inputxt(halo)
if inputxt(halo) do;
  use(mencetak teks)
else;
  pr("gagal")
continue;

inc(mencetak teks)
wait for(input)
if response do;
else;
  pr("yah gagal")
pr("halo juga")
endinc;
